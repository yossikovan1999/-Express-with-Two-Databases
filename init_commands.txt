npm init -y 

npm i express

npm i nodemon  - (start : nodemon app.js)

npm install dotenv --save 
import 'dotenv/config'
use with the following command - process.env

echo > .gitignore



when updating this is what is returned.
 {
  acknowledged: true,
  modifiedCount: 1,
  upsertedId: null,
  upsertedCount: 0,
  matchedCount: 1
}

